#include <stdio.h>
#include <string.h>

int main()
{
    int value;
    char str1[]="hello";
    char str2[]="Welcome";
    
    value=strcmp(str1,str2);
    if(value==0)
    {
        printf("same");
    }
    else
    printf("%d",value);

    return 0;
}
